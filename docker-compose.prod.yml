services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: lumilaya
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - nginx-network

# Réseaux externes existants
networks:
  nginx-network:
    external: true
